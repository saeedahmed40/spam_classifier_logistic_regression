# spam_classifier_logistic_regression
This project uses machine learning to identify spam emails. It's built in Python and uses the Enron dataset to train a model that can recognise patterns in spam. 
